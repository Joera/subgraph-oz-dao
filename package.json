{
  "name": "open-zeppelin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npx graph-compiler --config configs/simple-poll.json --include node_modules/@openzeppelin/subgraphs/src/datasources --export-schema --export-subgraph",
    "deploy": "npx graph-cli codegen generated/simple-poll.subgraph.yaml"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/subgraphs": "^0.1.7-1"
  }
}
